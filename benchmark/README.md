# Benchmark

Evaluation and benchmarking utilities for the Fast-SNOW pipeline.

## Structure

- `dataset_extract/` — Dataset preprocessing and extraction scripts
- `eval/` — Evaluation metrics and scoring
- `infer/` — Inference scripts for benchmark runs
